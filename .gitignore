# Claude Code local settings
.claude/

# Terraform state - never commit. Holds resource attributes in cleartext.
*.tfstate
*.tfstate.*

# Provider plugins. The lock file is committed - it pins the provider version.
.terraform/

# Submission archive
terraform.zip
